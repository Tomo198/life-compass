{
  "name": "life-compass",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "dev:worker": "npm run build && wrangler dev",
    "build": "tsc && vite build",
    "deploy": "npm run build && wrangler deploy",
    "preview": "vite preview --host 127.0.0.1",
    "test:logic": "node scripts/run-logic-tests.mjs",
    "test:worker-api": "node scripts/run-worker-api-tests.mjs",
    "test:e2e": "npm run build && node scripts/run-e2e.mjs",
    "test:e2e:headed": "npm run build && node scripts/run-e2e.mjs --headed",
    "verify": "npm run test:logic && npm run test:worker-api && npm run test:e2e"
  },
  "dependencies": {
    "jose": "^6.2.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0",
    "wrangler": "^4.106.0"
  }
}
